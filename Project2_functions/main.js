// Initialize two parts of the message
let part1 = "Hello, ";
let part2 = "Taiwo!";

// Start with the first part, then append the second
let finalMessage = part1;
finalMessage += part2;  // uses += to concatenate

// Find the element by its id and display the result
document.getElementById("message").textContent = finalMessage;

